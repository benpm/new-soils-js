# TODO - TerrainLab

Commit each task. Test before commit. Each header is a different project, likely needing its own branch. After completing tasks, move them to CHANGELOG.md

* [ ] Alternate 3D view: semi-offline render using GPU raymarching to show a very extended view of the large-scale terrain. Allow the camera to be moved, but render at much lower res, then progressively refine as camera stops moving. Use depth fog to give a sense of scale
