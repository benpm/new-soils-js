# Entity kinds (plan-game-systems §2). Kind ids are the declaration index
# (u16), so ordering is part of the protocol — append, never reorder.
- name: Player
  half_extents: [0.3, 0.9, 0.3]
  speed: 8.0
  render: capsule
- name: Critter
  half_extents: [0.35, 0.35, 0.35]
  speed: 2.0
  render: cube
